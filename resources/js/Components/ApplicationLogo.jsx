export default function ApplicationLogo({ width, height }) {
    return <>
        <img src="../../../storage/images/logo.png" style={{width: width + 'px', height: height + 'px'}} alt=""></img>
    </>;
}
